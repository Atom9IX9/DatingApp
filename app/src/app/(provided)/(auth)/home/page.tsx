import { AuthPreview } from "@/widgets/authPreview";
import { Box } from "@mui/material";

const Home = async () => {
  return (
    <Box>
      <AuthPreview />
    </Box>
  );
};

export default Home;
